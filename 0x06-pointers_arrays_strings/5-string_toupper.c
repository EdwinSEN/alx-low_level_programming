#include "main.h"

/**
 * string_toupper  ->converting any string to upper case
 * @x: string param
 * Return: string
 */
char *string_toupper(char *X)
{
	int i = 0;

	while (x[i])

             if (X[i] >=97 && x[i] <= 122)
                 X[i] = x[i] - 32;
               i++;	
	return (s);
}
